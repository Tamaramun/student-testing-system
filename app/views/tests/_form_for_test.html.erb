<%= form_for @test, class: 'form-horizontal' do |f| %>
  <div>
    <%= f.label :name %><br/>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
    <div>
      <%= f.label :description %><br/>
      <%= f.text_area :description, class: 'form-control' %>
    </div>
  <div>
    <%= f.submit 'Save', class: 'btn btn-success' %>
  </div>
<% end %>